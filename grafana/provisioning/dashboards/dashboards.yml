apiVersion: 1

providers:
  - name: 'LLM Gateway Platform Assets'
    orgId: 1
    folder: 'NOC Core Operations'
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards